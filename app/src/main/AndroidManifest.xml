<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.arafat_213.e_policephase2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".DashboardActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".PolicemenListActivity"
            android:label="@string/title_activity_policemen_list"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".AddPolicemanActivity" />
        <activity android:name=".NewNotificationActivity" />
        <activity android:name=".NotificationActivity"></activity>
    </application>

</manifest>